{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }} fade show alert-message text-center" role="alert">
            {% for message in messages %}
            <p class="text-center font-weight-bold">{{ message|raw }}</p>
        </div>
        {% endfor %}
    </div>
{% endfor %}
